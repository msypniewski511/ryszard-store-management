%h3 Products
%table
  %thead
    %tr
      %th Name
      %th Description
      %th Company
      %th Category
      %th Subcategory
      %th Price
      %th{ colspan: 3 } Action

  %tbody.index
    - @products.each do |product|
      %tr
        %td= link_to product.name, product, remote: true
        %td= product.description
        %td= product.company.name
        %td= product.category.name
        %td= product.subcategory.name
        %td= product.price
        %td
          |
          = link_to 'Edit', edit_admin_product_path(product), remote: true
          |
          = link_to 'Destroy', product, method: :delete, data: { confirm: "Are you sure to delete product: #{product.name} ?" }, remote: true, class: "destroy"
    %tr
      %td
        = link_to 'New product', new_admin_product_url, remote: true
      %td{colspan: 6}