= form_for @admin_company do |f|
  - if @admin_company.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_company.errors.count, "error")} prohibited this category from being saved:"
      %ul
        - @admin_company.errors.full_messages.each do |msg|
          %li= msg
= form_for @admin_company do |f|
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .action
    = f.submit
