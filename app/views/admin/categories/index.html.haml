%h3 Categories
%table
  %thead
    %tr
      %th Name
      %th Description
      %th{ colspan: 3 } Action

  %tbody.index
    - @categories.each do |category|
      %tr
        %td= category.name
        %td= category.description
        %td
          = link_to 'Show', category, remote: true
          |
          = link_to 'Edit', edit_admin_category_path(category), remote: true
          |
          = link_to 'Destroy', category, method: :delete, data: { confirm: "Are you sure to delete category: #{category.name} ?" }, remote: true, class: "destroy"
    %tr
      %td
        = link_to 'New Category', new_admin_category_url, remote: true
      %td{colspan: 4}