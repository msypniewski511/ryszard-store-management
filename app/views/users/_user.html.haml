%li
  = gravatar_for user, size: 50
  = link_to user.name, user
  - if current_user.admin? && !current_user?(user)
    |
    = link_to "usuń", user, method: :delete,
                                  data: { confirm: "Jesteś pewien aby usunąć użytkownika #{user.name}?" }